const assert=require('assert');const{VersionMaturityControl,LEVELS}=require('../src/version_maturity_control');
(async()=>{const v=new VersionMaturityControl();const capabilities={};const tests={};const evidence={};for(const k of LEVELS.L1.requires){capabilities[k]=true;tests[k]=true;evidence[k]=true;}for(const k of LEVELS.L2.requires){capabilities[k]=true;tests[k]=true;evidence[k]=true;}const a=v.evaluate({capabilities,tests,evidence});assert.equal(a.highestCertifiedLevel,'L2');assert.equal(a.levels.L3.passed,false);const c=await v.certify({version:'1.0.0',level:'L2',assessment:a,commitSha:'abc'});assert.equal(c.status,'certified');const blocked=await v.certify({version:'1.0.0',level:'L3',assessment:a,commitSha:'abc'});assert.equal(blocked.status,'blocked');assert.equal(v.releaseLabel({major:1,minor:2,patch:0,level:'L3',channel:'beta'}),'v1.2.0-l3.beta');assert.equal(v.releaseLabel({major:2,minor:0,patch:0,level:'L5',channel:'stable'}),'v2.0.0-l5');console.log('version_maturity_control.test.js passed');})().catch(e=>{console.error(e);process.exit(1)});
