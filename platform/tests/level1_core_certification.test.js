const assert=require('assert');const{Level1CoreCertification}=require('../src/level1_core_certification');
(()=>{const engine=new Level1CoreCertification();const full=Object.fromEntries(engine.requiredChecks.map(k=>[k,{implemented:true,tested:true,evidence:true}]));const certified=engine.audit(full);assert.equal(certified.certification,'CERTIFIED');assert.equal(certified.score,100);const incomplete={...full,backup_restore:{implemented:true,tested:false,evidence:false}};const blocked=engine.audit(incomplete);assert.equal(blocked.certification,'BLOCKED');assert.ok(blocked.blockers.includes('backup_restore'));const plan=engine.hardeningPlan(blocked);assert.ok(plan[0].actions.includes('add deterministic automated tests'));assert.equal(engine.releaseDecision({audit:certified,ciPassed:true,rollbackVerified:true,ownerApproved:true}).decision,'GO');const noGo=engine.releaseDecision({audit:certified,criticalSecurityFindings:1,ciPassed:true,rollbackVerified:true,ownerApproved:true});assert.equal(noGo.decision,'NO_GO');assert.ok(noGo.blockers.includes('critical_security_findings'));console.log('level1_core_certification.test.js passed');})();
