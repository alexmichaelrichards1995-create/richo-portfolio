{
  "name": "richo-github-app",
  "version": "0.1.0",
  "private": true,
  "description": "R.I.C.H.O. GitHub App and Marketplace SaaS core",
  "type": "commonjs",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "start": "node src/hardened-entrypoint.js",
    "migrate": "node scripts/migrate.js",
    "dev": "node --watch src/hardened-entrypoint.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/hardened-entrypoint.js && node --test"
  },
  "dependencies": {
    "pg": "8.22.0"
  }
}
